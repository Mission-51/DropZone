spring:
  datasource:
    url: jdbc:mysql://mysql-container:3306/dropzone-mysql
    username: root
    password: dropzoned110
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  redis:
    host: redis-container
    port: 6379